services:
  service-name:
    image: $IMAGE
    environment:
      - APP_ENV=$APP_ENV
      - API_HOST=$API_HOST
      - API_PORT=$API_PORT
      - API_VERSION=$API_VERSION
      - JWT_SECRET_KEY=$JWT_SECRET_KEY
      - DATABASE_URL=$DATABASE_URL
    ports:
      - "$API_PORT:$API_PORT"